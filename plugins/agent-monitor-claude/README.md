# AgentMonitor Claude Code Plugin

This package records Claude Code lifecycle events through plugin-bundled hooks.

Install from this repo marketplace:

```bash
claude plugin marketplace add /Users/caopu/workspace/AgentMonitor
claude plugin install agent-monitor@agent-monitor-local
```

Run `/reload-plugins` or restart Claude Code after installation.
